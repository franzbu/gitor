{
  "name": "gitor",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "generate-content": "node generate-content.js",
    "predev": "npm run generate-content",
    "dev": "astro dev",
    "build": "npm run generate-content && astro build && node verify-build.js",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "overrides": {
    "vite": "^7"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.8",
    "wrangler": "^4.112.0"
  }
}
